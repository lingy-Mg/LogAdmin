# LogAdmin 快速使用指南

## 🚀 三种运行模式

### 1️⃣ 开发模式（推荐开发时使用）

**前后端分离，热重载**

终端1 - 启动后端：
```bash
npm run dev:backend
```

终端2 - 启动前端：
```bash
npm run dev:frontend
```

- 后端: http://localhost:3000
- 前端: http://localhost:5173 ✅ 访问这个

### 2️⃣ 生产模式（推荐部署时使用）

**一体化运行，单端口**

```bash
# 1. 打包（首次或代码更新后）
npm run build

# 2. 启动
npm start
```

- 访问: http://localhost:3000 ✅ 前后端合一

### 3️⃣ 一键模式（Windows快捷方式）

**双击运行**

```bash
# 打包
build.bat

# 启动
start.bat
```

## 📦 npm 命令大全

```bash
# 安装所有依赖
npm run install:all

# 开发模式
npm run dev:backend     # 启动后端开发服务器
npm run dev:frontend    # 启动前端开发服务器

# 生产构建
npm run build           # 完整构建（前端 + 复制）
npm run build:frontend  # 仅构建前端
npm run copy:dist       # 仅复制文件

# 启动
npm start              # 启动后端（生产模式）

# 清理
npm run clean          # 删除所有构建文件
```

## 🎯 典型工作流程

### 开发阶段

```bash
# 第一次使用
1. npm run install:all   # 安装依赖
2. npm run dev:backend   # 终端1
3. npm run dev:frontend  # 终端2
4. 访问 http://localhost:5173

# 日常开发
- 修改代码自动热重载
- 后端改动需重启 dev:backend
- 前端改动自动刷新
```

### 测试部署

```bash
1. npm run build        # 打包
2. npm start            # 启动
3. 访问 http://localhost:3000
```

### 生产部署

```bash
# 本地
1. npm run build        # 打包

# 服务器
2. 上传 backend/ 目录
3. cd backend && npm install
4. npm start

# 或使用 PM2
5. pm2 start server.js --name logadmin
```

## 🔍 目录说明

```
LogAdmin/
├── frontend/          # 前端源码
│   ├── dist/         # 构建产物（临时）
│   └── src/          # 源代码
├── backend/          # 后端源码
│   ├── public/       # 前端静态文件（部署用）
│   ├── server.js     # 服务器主文件
│   ├── logs-data.json          # 日志数据
│   └── device-aliases.json     # 设备别名
├── scripts/          # 构建脚本
├── build.bat         # 一键打包
└── start.bat         # 快速启动
```

## ⚙️ 配置说明

### 端口配置

**开发模式:**
- 后端: `backend/server.js` 第114行 `const PORT = 3000`
- 前端: `frontend/vite.config.js` 第6行 `port: 5173`

**生产模式:**
- 统一端口: `backend/server.js` 第114行

### API地址配置

**开发模式:**
`frontend/src/App.vue` 第100行:
```javascript
const socket = io('http://localhost:3000')
```

**生产模式:**
打包后自动使用当前域名，无需修改。

## 🐛 常见问题

### Q: 打包后页面空白？
**A:** 检查 `backend/public/` 目录是否有文件，运行 `npm run build` 重新打包。

### Q: WebSocket 连接失败？
**A:** 
- 开发模式: 确认后端 3000 端口已启动
- 生产模式: 检查防火墙设置

### Q: 端口被占用？
**A:** 修改 `backend/server.js` 中的 PORT 配置。

### Q: 修改代码不生效？
**A:**
- 开发模式: 前端自动刷新，后端需重启
- 生产模式: 运行 `npm run build` 重新打包

## 📊 性能建议

### 开发环境
- ✅ 使用开发模式（热重载快速）
- ✅ 打开浏览器开发工具查看日志

### 生产环境
- ✅ 使用生产模式（性能更好）
- ✅ 关闭模拟日志功能
- ✅ 配置 PM2 进程管理
- ✅ 使用 Nginx 反向代理

## 🎉 开始使用

**首次使用三步走:**

```bash
1. npm run install:all     # 安装依赖
2. npm run dev:backend     # 终端1
3. npm run dev:frontend    # 终端2
```

访问 http://localhost:5173 开始体验！

**部署三步走:**

```bash
1. npm run build          # 打包
2. cd backend             # 进入后端
3. npm start              # 启动
```

访问 http://localhost:3000 查看生产版本！
